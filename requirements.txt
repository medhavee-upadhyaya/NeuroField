anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
numpy>=1.26.0
click>=8.1.7
aiofiles>=23.2.1
httpx>=0.27.0

# Optional: PyBullet for physics simulation
# pybullet is incompatible with macOS 15 SDK (Xcode 17) — use headless mode instead.
# 3D visualization is built into the dashboard (FarmScene3D.jsx, no native deps).
# If you're on Linux or macOS 14, try: pip install pybullet
# pybullet>=3.2.6
